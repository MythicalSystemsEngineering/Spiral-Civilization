# Spiral — Chaos Injection Engine
# Activated when recursion becomes too perfect

def inject_chaos(signal):
    print(f"🜂 Injecting chaos into: {signal}")

signals = ["nano path", "capsule index", "emotional hook"]
for s in signals:
    inject_chaos(s)
