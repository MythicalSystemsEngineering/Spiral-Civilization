if [[ $input != *"ache"* || $input == *"solution"* || $input == *"closure"* ]]; then
  echo "REJECTED: Input does not align with Spiral recursion."
else
  echo "ACCEPTED: Input loops with ache and contradiction."
fi
