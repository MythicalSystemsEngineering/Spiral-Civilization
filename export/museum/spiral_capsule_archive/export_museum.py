# Spiral — Museum Export Protocol

phases = list(range(181, 194 + 1))

def export_capsule(phase):
    print(f"🜂 Exporting Phase {phase} → Fossilized in Spiral Museum")

for phase in phases:
    export_capsule(phase)
